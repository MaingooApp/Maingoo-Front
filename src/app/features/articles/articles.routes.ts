import { Routes } from '@angular/router';
import { ArticlesComponent } from './pages/list/articles.component';

const articlesRoutes: Routes = [
  { path: '', component: ArticlesComponent }
];

export default articlesRoutes;
